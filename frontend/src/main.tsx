import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="p-6">
      <h1 className="text-2xl font-bold">Frontend Initialized Successfully</h1>
    </div>
  </React.StrictMode>,
)
